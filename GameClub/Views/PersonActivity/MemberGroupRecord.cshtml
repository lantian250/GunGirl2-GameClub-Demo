@using GameClub.Models
@model IEnumerable<MemberGroup>
@{

    ViewBag.Title = "个人分组历史记录";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    int count = 0;
}

@section main{
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-header">
                        <h4>个人分组历史记录(仅显示最近20条信息)</h4>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table table-bordered">
                                <thead>
                                    <tr>
                                        <th>编号</th>
                                        <th>@Html.DisplayNameFor(m => m.MemberGroupID)</th>
                                        <th>@Html.DisplayNameFor(m => m.GroupName)</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>@(++count)</td>
                                            <td>@item.MemberGroupID</td>
                                            <td>@item.GroupName</td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>          
            </div>
        </div>
    </div>
}

@section script{

}